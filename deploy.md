# Vercel Environment Variables

| Variable | Value |
|---|---|
| `VITE_API_BASE` | `https://mini-crm-yhud.onrender.com/api` |

Set these in Vercel → Project Settings → Environment Variables, then redeploy.
